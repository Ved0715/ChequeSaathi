# Database (Neon PostgreSQL)
# Pooled connection for app (fast, for API requests)
DATABASE_URL="postgresql://username:password@your-project-pooler.region.aws.neon.tech/dbname?sslmode=require"

# Direct connection for migrations (full PostgreSQL features)
DIRECT_URL="postgresql://username:password@your-project.region.aws.neon.tech/dbname?sslmode=require"

# Server
PORT=5000

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRE=7d

# CORS
FRONTEND_URL=http://localhost:3000

# Email Service (Optional)
RESEND_API_KEY=

# File Storage (Optional)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
